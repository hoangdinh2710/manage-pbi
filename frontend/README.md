# Frontend App

React + Vite dashboard for interacting with the Power BI management API.

## Local setup

1. `npm install`
2. `npm run dev`

The dev server proxies API requests to `http://localhost:8000`.
